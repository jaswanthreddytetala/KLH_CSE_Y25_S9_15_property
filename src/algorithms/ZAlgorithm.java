package algorithms;

import java.util.List;

public class ZAlgorithm {
    public static int[] zValues(String text) {
        return algorithms.string.ZAlgorithm.zValues(text);
    }

    public static List<Integer> findMatches(String text, String pattern) {
        return algorithms.string.ZAlgorithm.findMatches(text, pattern);
    }
}
