package algorithms;

import java.util.List;

public class RabinKarp {
    public static List<Integer> search(String text, String pattern, long base, long modulus) {
        return algorithms.string.RabinKarp.search(text, pattern, base, modulus);
    }
}
