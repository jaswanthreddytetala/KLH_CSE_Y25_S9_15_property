package algorithms;

import java.util.List;

public class LCPArray {
    public static List<Integer> build(String text, List<Integer> suffixArray) {
        return algorithms.string.LCPArray.build(text, suffixArray);
    }
}
